#pragma once

#include "../types.hpp"
#include "../../libs/of.hpp"

namespace atlas {
    namespace gui {
        class Layers {
        public:
            void draw(ofRectangle rect, Events events);
        };
    }
}
